"""Runner scripts and shared objective utilities.

Each runner script is intended to be invoked directly from the command
line (from the repository root), not imported. The shared helpers in
:mod:`scripts.objective` are the one exception — they are imported by
every runner.
"""
